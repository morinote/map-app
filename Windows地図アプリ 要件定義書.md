### Windows地図アプリ 要件定義書

#### 1\. プロジェクト概要

本プロジェクトの目的は、開発者本人が個人用途で利用するための、Windowsデスクトップ環境に最適化された地図管理アプリケーションを構築することである。無料の地図ライブラリ「Leaflet」を中核とし、地図上への地点情報の記録（マーカー管理）および柔軟な表示切り替えを低コストかつ効率的に実現する。

#### 2\. ターゲットユーザーおよび利用シーン

本システムは、開発者自身が唯一の利用者となるパーソナルツールとして定義する。

* **ターゲットユーザー**  
* 開発者本人（1名）  
* **利用形態**  
* **メイン環境** : Windows PCのブラウザ上での日常的な地点閲覧・編集。  
* **検証・サブ環境** : スマートフォン端末を用いた外出先での動作チェックおよび表示確認。  
* **体験の最適化** : GitHub PagesによるWeb配信ながら、ブラウザのショートカット機能やPWA（Progressive Web App）化を検討し、Windowsデスクトップアプリに近い操作感を実現する。

#### 3\. 機能要件

本システムが備えるべき機能の詳細は以下の通りである。

##### 3.1 マーカー管理機能

ユーザーは地図上の任意の地点を直感的に管理できるものとする。

* **追加** : 地図上のクリック（またはタップ）操作により、その地点の座標を取得し新規マーカーを設置する。設置時には名称やカテゴリを入力するポップアップを表示する。  
* **削除** : 既存のマーカーを選択し、詳細情報ポップアップ内の削除ボタンから即座に破棄できるものとする。

##### 3.2 カテゴリ・フィルター機能

多数のマーカーを効率的に整理するため、分類・抽出機能を実装する。

* **カテゴリ分類** : 各マーカーに対して、ユーザー定義のカテゴリ（例：「飲食店」「作業場所」等）を割り当てる。  
* **フィルター動作** : サイドバーまたはコントロールパネル上のチェックボックス等により、カテゴリ単位でマーカーの表示・非表示を即座に切り替える。

##### 3.3 データ永続化要件（技術的補足）

本アプリは静的ホスティング（GitHub Pages）を利用するため、サーバーサイドのデータベースを持たない。

* **保存方式** : ブラウザの  **LocalStorage**  または  **IndexedDB**  を活用し、登録したマーカー情報をクライアント側に保存する。  
* **整合性** : ページのリフレッシュ後も、前回登録したマーカーが自動的に再描画される仕組みを構築する。

##### 3.4 機能一覧

機能名,内容,備考  
マーカー管理,地図上へのマーカー追加・削除および名称設定,座標取得は地図クリックをトリガーとする  
カテゴリ分類,各マーカーへの属性付与（1マーカー1カテゴリ）,データ構造はJSON形式で定義し拡張性を保持  
フィルター機能,カテゴリ別の表示・非表示切り替え,Leafletの LayerGroup を用いて制御  
データ永続化,ブラウザへの地点データ保存・読み込み,LocalStorageを使用。将来的にはJSONエクスポートも検討

#### 4\. 技術スタック

コストパフォーマンスと開発効率、およびWindows環境での親和性を考慮し、以下の技術を選定する。

* **地図ライブラリ** : Leaflet  
* **選定理由** : オープンソース（無料）であり、軽量かつモバイルフレンドリーな設計であるため。また、プラグインが豊富で、レイヤー管理などの拡張が容易な点も評価した。  
* **ホスティング** : GitHub Pages  
* **選定理由** : 無料かつ高速な静的配信が可能であり、GitHubのリポジトリと直結しているためバージョン管理とデプロイの手順を簡略化できる。  
* **フロントエンド技術** : HTML5, CSS3, JavaScript (Vanilla JS or Modern Framework)  
* **選定理由** : 特定のOSに依存せず、Windows上の主要ブラウザ（Edge, Chrome）で最新のWeb APIを活用し、「アプリ体験」を提供するため。

#### 5\. 動作環境

ウェブ標準技術をベースとし、以下の環境をサポート対象とする。

* **メイン環境** : Windows PC  
* **対応ブラウザ** : Microsoft Edge, Google Chrome（最新版）  
* **操作体系** : マウス操作およびキーボードショートカット  
* **検証環境** : スマートフォン（iOS / Android）  
* **対応ブラウザ** : Safari, Google Chrome  
* **操作体系** : タッチ操作、GPS連携による現在地確認（任意）

#### 6\. 制約事項・その他

* **ライブラリ制約** : 完全無料での運用を維持するため、商用利用制限のない、またはMITライセンス等の無料ライブラリのみを使用する。  
* **運用形態** : 開発者と利用者が同一であるため、複雑なユーザー認証や権限管理機能は一切排除し、シンプルかつ軽量な構成を維持する。  
* **オフライン動作** : 原則としてオンライン環境を前提とするが、Leafletのキャッシュ機構により一部オフラインでの閲覧性を考慮する。

